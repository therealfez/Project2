<div class="container" id="theory-container">
  <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">
          <h5 class="card-title">{{example.text}}</h5>
            <h5><i class="fas fa-eye hidden"></i><i class="fas fa-eye hidden"></i></h5>
          <p class="description">
            {{example.description}}</p>
          <p>
          <p class="source">
            <strong>Source</strong>: {{example.source}}</p>
            <div class="reliability">
              {{#if sources}}
                <h1>{{type}}</h1>
              {{else}}
                <h1>Source Is Reliable</h1>
              {{/if}}
          </div>
          <p class="tags">
            <strong>Tags</strong>: {{example.tag}}</p>
            
          <p class="back"><a href="/home">← Back To Home</a></p>
        </div>
      </div>
    </div>
  </div>
</div>
